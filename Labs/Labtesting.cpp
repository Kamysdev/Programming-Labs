#include <math.h>
#include <stdio.h>
#include <iostream>	


/*
int main()
{
	int count, n, Pn, count1, count2, Pi; count = 5;

	for (Pi = 1, count2 = count; count2 > 0; count2--)
		Pi = Pi * count2;
		printf("%d\n", Pi);
}
*/